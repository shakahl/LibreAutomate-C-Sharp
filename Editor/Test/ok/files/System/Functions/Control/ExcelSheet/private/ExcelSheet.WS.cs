function [flags] ;;flags 1 autoinit (fbc)

if(ws) ret
if(flags&1=0) end ERR_INIT

end F"ExcelSheet variable not initialized. You should call Init before other functions. This function calls Init for backward compatibility." 8|2
Init
err+ end ERR_INIT

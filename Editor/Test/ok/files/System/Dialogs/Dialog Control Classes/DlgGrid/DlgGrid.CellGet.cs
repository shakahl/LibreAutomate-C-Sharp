function$ row column

 Returns cell text.
 Returns 0 if failed.
 The return value is temporary.
 Tip: If need the value as a number, use function val to convert: int i=val(x.CellGet(r c))

 row - 0-based row index.
 column - 0-based column index.


ret RowGetMS(row 1 column)

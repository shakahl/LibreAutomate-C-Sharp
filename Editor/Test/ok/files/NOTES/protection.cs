Send user info.
   Only if 'Check for updates' is checked, which is checked by default.
   Later, if all ok, check always, and maybe do something more.

Time bombs.

Temporary regcode: don't allow to use temp regcode more than one time.

After some usage, recognize crack. Then connect to a script in the qm website. The script could just log this, or could return command to disable QM for that user. But I don't know whether it is good idea.

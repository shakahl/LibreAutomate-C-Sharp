  Scroll_Up_MW: Scrolls the window that the cursor covers, using WM_MOUSEWHEEL
  Recommended trigger: #CAU
      (But #CAU won't work in Microsoft Word and programs that check if Ctrl and Alt
	  are held down as you scroll. In this case, use the other #U-triggerd instead of this.)
  IMPORTANT: make sure "Eat" in Properties is unchecked

Scroll_(1 0 1)		;;doesnt use factors, use Scroll(1 1) for factors

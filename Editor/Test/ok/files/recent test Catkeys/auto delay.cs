QM2 calls AutoDelay in:
act, clo, hid, ont, min/max/res, mov/siz, but, men,
paste, key, lef/etc,
run, web,
wait(-1), wait(WT),

QM2 uses get_speed in:
paste, copy,
lef/etc
GetPoint (lef/etc, mou, pixel)
opt slowmouse (MouseMoveTo (lef/etc, mou, scan))
scan (response time, autodelay)
mou
key
wait(C)
act,
but,
win,
